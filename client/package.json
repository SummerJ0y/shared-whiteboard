{
  "name": "whiteboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --hostname 0.0.0.0",
    "dev:deploy": "dotenv -e .env.deployed-backend.local -- bash -c 'next dev --hostname $HOST'",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.2.4",
    "perfect-freehand": "^1.2.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-resizable-panels": "^2.1.7",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
